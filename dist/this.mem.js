const MEMORY_TEMPLATE = [];
global.thismem = {
    flush: function(n) {
     //  var lc = Memory.thismem.flushCount;
     //  if (lc === undefined) {
     //    Memory.thismem.flushCount = 0
     //    lc = Memory.thismem.flushCount;
     //    console.log('memory: Memory Flush Cont undefined')
     //    this.logger.queueMessage('memory', 'flush count undefined')
     //  } else {
     //   lc = Memory.thismem.flushCount;
     //   Memory.thismem.flushCount = lc + n;
     //   console.log('memory: flush trigger')
     //   this.logger.queueMessage('memory', 'Flush trigger')

     //  }
    }
}
