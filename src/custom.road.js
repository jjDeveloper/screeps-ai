var customRoad = {
  run: function(creep){
    // var custPath = PathFinder.search(Game.spawns['StrikerCentral'].pos, Game.getObjectById('579faa260700be0674d307fe').pos);
    // //console.log(creep.pos);
    // //var creepPath = PathFinder.search(creep.pos, creep.pos);
    // for( var i in custPath){
    //
    // }
     // console.log(creepPath.path);
      //var pos = new RoomPosition(creepPath.path[i].x, creepPath.path[i].y, creepPath.path[i].roomName);
      // if (creep.pos.createConstructionSite(STRUCTURE_ROAD) != OK) {
      //   return;
      // }
   // }

  }
}
module.exports = customRoad;

